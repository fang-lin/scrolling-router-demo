import React from 'react';
import TitleBar from "./TitleBar";

const Feedback = () => {
    return (
        <div>
            <TitleBar title="Feedback" />
        </div>
    );
}

export default Feedback;